import React from 'react'
import classes from './styles.module.scss';

interface ISearchItemProps {

}

export const SearchItem = (props: ISearchItemProps) => {
return (<div className={classes.searchItemContainer}>
</div>)
}